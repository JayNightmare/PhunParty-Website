# API Configuration
VITE_API_URL=your_api_url_here
VITE_API_KEY=your_api_key_here

# WebSocket Configuration
VITE_WS_URL=wss://your_websocket_url_here

# Development Settings (for development proxy)
# In development, you can use localhost:8000 if running backend locally
# VITE_API_URL=http://localhost:8000
# VITE_WS_URL=ws://localhost:8000

# Important Notes:
# 1. Make sure VITE_API_KEY matches the backend's API_KEY
# 2. WebSocket connections cannot use API keys in headers, so they rely on server configuration
# 3. The backend WebSocket endpoint should not require API key authentication
# 4. In production, make sure the server supports WebSocket connections